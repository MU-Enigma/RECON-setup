# File to be placed in /usr/local/sbin/
# Some functions to be run during new user creation
# Author: Srikar (aka epoch101)

# $1 - username 
# $2 - uid
# $3 - gid
# $4 - home-dir

MOD_DIR=/usr/local/sbin/adduser_mod

# Add user to LDAP
$MOD_DIR/ldif_editors/genLDIF.sh $1 $2 $3

# Generate and add ssh-keys for new user
$MOD_DIR/ssh-keymgmt/gen_newuser_keys.sh $1 $2 $3