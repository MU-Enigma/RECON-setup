# File to be placed in /usr/local/sbin/
# Some funtions to be run after the deletion of a user
# Author: Srikar (aka epoch101)

# $1 - username
# $2 - uid
# $3 - gid
# $4 - home-dir

MOD_DIR=/usr/local/sbin/deluser_mod

# Delete user from LDAP
$MOD_DIR/rm_ldap.sh $1 $2 $3

# Delete home directory if not deleted
if [ -d "/home/$1" ]; then
	rm -fr /home/$1
fi